---
title: "Schemas"
description: "Data shapes a Framework or Playbook produces or consumes in the verified marketing knowledge base — never real client data."
type: Knowledge Base Folder
status: stable
tags:
  - cross-cutting
  - knowledge-base
---

# Schemas

Data shapes only — the *structure* a Framework or Playbook needs to be applied to a real business (field names, ordering), never a real client's actual data. See [structural-decisions.md](../../project-context/structural-decisions.md) point 4 for the full design reasoning, and each file's own "Shape" section for how much precision the transcript (or, for the one assembly schema below, the knowledge base's own already-cited structure) actually supports.

For the full, current list, see [the root index.md](../../index.md), kept there deliberately so there's exactly one place that goes stale if it's not updated.

## Changes

- 2026-09-13: this README added (a pre-existing gap — the sibling `frameworks/`, `playbooks/`, and `principles/` folders already had one) alongside [Marketing Plan Structure](marketing-plan-structure-diagnosis-strategy-tactics-assembly.md), the first Schema in this folder built entirely as an assembly of already-cited concepts rather than extracted from a new transcript passage — see that file's own "Citation status" section for why.